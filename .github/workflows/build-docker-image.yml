name: Build Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        run: docker build . --file pw/pw-jwt-oauth/server/src/main/docker/Dockerfile --tag pw-jwt-oauth:$(date +%s)